# Path
path: your_path/dataset/kitti  # dataset root dir
train: images/train_set  # train images (relative to 'path')
val: images/val_set  # val images (relative to 'path')

# Classes
nc: 3  # number of classes
names: ['Car', 'Pedestrian', 'Cyclist'] # class names
